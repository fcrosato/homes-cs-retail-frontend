spring:
  application:
    name: homes-cs-retail-frontend
  cloud:
    config:
      uri: ${config.server.uri:http://${HOST:config.ci.prod.auction.local}:8888}
      label: ms
encrypt:
  failOnError: true
